# edit-medication-schedule

SRD-1169 edit slice: PUT /schedules/{schedule} with history-preservation semantics (same as delete), plus split dose generation into 14-day sync + 365-day async via queued job.
